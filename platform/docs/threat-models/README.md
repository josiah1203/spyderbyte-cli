# Threat models

The control-plane threat model covers tenant isolation, authority/approval races, secret handling,
artifact immutability, event replay, sandboxing, and worker replacement. Hosted residual risks are
listed explicitly for the deployment review.
