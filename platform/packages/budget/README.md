# Budget

The Phase 3 budget ledger accounts in integer minor units. It enforces workflow hard limits and
category limits under keyed serialization, supports reservations, partial consumption, release,
reconciliation, retry budgets, soft-limit alerts, and anomaly records. Every mutation requires an
invocation-bound authority envelope.
