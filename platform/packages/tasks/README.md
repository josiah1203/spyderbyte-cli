# Tier 2 tasks

The local vertical slice contains deterministic CSV/JSON profiling, schema checks, PII markers,
duplicate detection, deterministic train/validation/test assignment, and split-leakage detection.
Tasks return structured results and cannot create agents or broaden authority.
