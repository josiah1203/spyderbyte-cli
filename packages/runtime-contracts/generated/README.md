# Generated contracts

This directory contains deterministic JSON Schema bundles and human-readable contract output.
Regenerate it with `pnpm contracts:generate` from the repository root; CI must reject drift.
