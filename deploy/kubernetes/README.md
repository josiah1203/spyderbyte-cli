# Kubernetes deployment

Hosted scheduler manifests are intentionally deferred until the cloud scheduler and Kubernetes
versus SLURM decision gates are accepted. Provider-neutral scheduler and worker contracts are
implemented under `packages/backends`.
